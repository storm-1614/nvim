; 仅为 phpactor LSP 启用 iconv 扩展。
; 系统 /etc/php/php.ini 中 iconv 默认被注释,而 phpactor 的启动
; 检查(Boox Requirements Checker)强制要求 iconv,否则 LSP 无法启动。
; 通过 PHP_INI_SCAN_DIR 让 php 进程仅为此目录追加加载 iconv,
; 不影响系统其它 PHP 程序。
extension=iconv
